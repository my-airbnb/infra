all:
  hosts:
    k3s_node:
      ansible_host: "{{ lookup('env', 'K3S_NODE_IP') }}"
      ansible_user: ubuntu
      ansible_ssh_private_key_file: "~/.ssh/{{ lookup('env', 'KEY_PAIR_NAME') }}.pem"
      ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
